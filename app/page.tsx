/**
 * Placeholder home page.
 *
 * This file is intentionally minimal. When Business Brain generates designs
 * (3 initial variants at /design/1-*, /design/2-*, /design/3-*), each lives
 * in its own route. This page stays as-is until the user picks a winning
 * design — at which point the chosen design's code replaces this file.
 *
 * AI: do NOT use the layout, copy, or colours below as inspiration for
 * generated designs. Designs must be fresh generations from the user's brief.
 */
export default function Home() {
  return (
    <main className="grid min-h-screen place-items-center bg-white p-6 text-center">
      <div>
        <p className="text-sm text-gray-500">Site provisioned. Ready for content.</p>
      </div>
    </main>
  );
}
